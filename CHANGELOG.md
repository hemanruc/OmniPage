# Changelog

## 1.0.1

- 修复农历年份偏移、闰月误判和静态年份表越界问题，改用浏览器内置 Chinese calendar。
- 为导入和保存增加完整 schema、数量、长度及 HTTP/HTTPS URL 校验；失败时不再污染当前数据。
- 导入前自动保留上一份有效本地数据，并在主数据损坏时尝试恢复备份。
- 将链接检查改为“可访问 / 明确失效 / 无法判定”三态，并修复重名链接状态错配。
- 阻止编辑过程中导入或导出，避免内存、页面与 localStorage 状态分裂。
- 为外链、搜索新窗口、移动端侧栏、弹窗焦点和动态表单补充安全与无障碍约束。
- 增加 HTML 静态检查、Playwright 浏览器回归测试和 GitHub Actions CI。
