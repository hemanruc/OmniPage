# OmniPage 导航主页

> 你的个人网址导航页：一个文件，双击就能用，不用安装任何东西。

当前版本：**v1.0.1**。运行时仍只需要 `index.html`；仓库中的测试文件仅用于开发验证。

![截图](ScreenShot.png)

---

## 一、这个项目解决什么问题？

用浏览器书签收藏网站的人越来越多会遇到这些烦恼：

| 😣 浏览器书签的烦恼 | ✅ OmniPage 怎么解决 |
|----------------------|----------------------|
| 收藏的网站太多，书签栏一长串，找起来费劲 | 网站按**分类卡片**整齐排列，左边栏点分类名直接跳转 |
| 书签只能分文件夹，层级一多就乱，类别不清晰 | 分类一目了然，可随意**新增、改名、拖动排序**，按自己的习惯组织 |
| 书签只能存一个名字，想多写点备注没办法 | 每个网址支持**名称 + 网址 + 备注 + 关键词**多重信息，鼠标悬停即可查看 |
| 收藏久了，很多网站已失效也不知道 | 一键「**检查链接**」，区分可访问、明确失效和浏览器无法判定 |
| 换电脑、换浏览器，书签迁移麻烦 | 「**导出**」成一个文件，新设备上「**导入**」即可完整恢复 |

整个导航页的运行版**只是一个 HTML 文件**——不用安装、不用注册，也没有 OmniPage 数据服务器。书签数据保存在当前浏览器的 localStorage 中；使用搜索或链接检查时，浏览器会按需访问对应的第三方网站。

## 二、开始使用（两种方式任选）

两种方式都需要先下载文件：

**下载方法**：在本页面上方文件列表中点击 `index.html` → 进入文件页面后，点右上角的**下载按钮（⬇ 图标）**，文件就保存到电脑了（通常在「下载」文件夹里）。

### 方式 A：放在固定目录 + 设为浏览器书签/主页

适合：习惯从浏览器书签栏打开的人。

**第 1 步：把文件放到固定位置**

1. 打开「文件资源管理器」，进入 C 盘（`C:\`）
2. 在空白处右键 →「新建」→「文件夹」，起个名字，比如 `我的导航`
3. 把下载好的 `index.html` **剪切**进这个文件夹
   （最终路径类似 `C:\我的导航\index.html`）
   > ⚠️ 放好后就不要再移动或删除这个文件，否则书签会失效

**第 2 步：设为书签（放在书签栏最左边）**

1. 双击 `index.html`，用浏览器打开它
2. 按 `Ctrl + D`（Mac 按 `Cmd + D`）添加书签，保存位置选「**书签栏**」
3. 在书签栏上**按住这个新书签，拖到最左边**，以后一点即到

**（可选）第 3 步：设为浏览器主页，打开浏览器就是它**

- **Edge**：设置 →「开始、主页和新建标签页」→ 打开方式选「打开特定页面」→ 添加页面 → 填入文件路径，格式如 `file:///C:/我的导航/index.html`
- **Chrome**：设置 →「启动时」→「打开特定网页」→ 填入同样的 `file:///C:/我的导航/index.html` 路径

### 方式 B：桌面快捷方式，双击即用

适合：习惯从桌面打开的人。

1. 先按方式 A 的第 1 步，把 `index.html` 放到一个固定文件夹（比如 `C:\我的导航\`）
2. 在该文件上点**右键** →「显示更多选项」（Win10 可跳过）→「**发送到**」→「**桌面快捷方式**」
3. 桌面上会出现一个快捷方式图标，可以右键「重命名」改成「导航主页」
4. 以后**双击桌面图标**就能打开使用了

> 💡 数据由“浏览器配置文件 + 页面来源/文件路径”共同隔离。固定 `index.html` 的位置并始终使用同一浏览器配置文件，最不容易出现数据看似丢失的情况。

## 三、一步步教你用每个功能

### 🔍 功能 1：搜索

1. 在页面顶部找到搜索框
2. 输入你想搜的内容（比如「红烧肉做法」）
3. 按**回车键**，或点右边的「**搜索**」按钮
4. 想换搜索引擎？点搜索框下面的「夸克 / 必应 / 谷歌 / 淘宝 / 京东」标签即可，选中的会有红圈标记

### 📂 功能 2：打开收藏的网址

- 页面上一个个小卡片就是网站链接，**点击卡片**即可在新标签页打开
- 左边栏是分类目录，点分类名字可以快速跳到对应区域
- 鼠标悬停在卡片上，可以看到完整网址、备注和关键词

### ✏️ 功能 3：添加 / 修改网址（最重要！）

**第一步：进入编辑模式**
点左边栏下方的红色「**编辑**」按钮。

**第二步：你会看到页面变了**
- 每个网址卡片变成了可以输入文字的格子
- 左边栏出现「保存」和「取消」按钮

**第三步：想做什么就做什么**

| 我想…… | 怎么做 |
|--------|--------|
| 改某个网址的名字 | 直接在卡片的第一个格子里修改 |
| 改网址地址 | 在第二个格子（URL）里修改；可省略协议，保存时会自动补为 `https://` |
| 加备注/关键词 | 在第三、四个格子里填写（可选，方便以后辨认） |
| 添加新网址 | 滚动到该分类最下面，点「➕ 添加链接」，然后填写 |
| 删除某个网址 | 点卡片上的红色「删除」按钮 |
| 调整网址顺序 | 按住卡片不放，**拖动**到想要的位置再松手 |
| 新增一个分类 | 滚动到页面最底部，点「添加新分类」 |
| 改分类名字 | 直接修改分类标题那一栏的文字 |
| 调整分类顺序 | 点分类标题右边的 ↑ ↓ 箭头 |
| 删除整个分类 | 点分类标题右边的「删除」（会弹窗确认） |

**第四步：保存！**

改完后点左边栏的绿色「**保存**」按钮，看到「已保存到本地」提示就完成了。

> ⚠️ 如果点「取消」，刚才的所有修改都会丢弃，恢复到编辑前的样子。

### 📅 功能 4：看日期和农历

页面顶部自动显示今天的公历日期、星期几和农历，不用任何操作。

### 🔗 功能 5：检查网址是否失效

1. 点左边栏的「**检查链接**」按钮
2. 等待一会儿（网址多的话需要一两分钟），检查中的卡片右上角会有黄色小点在闪
3. 检查完成后会出现三种状态：**绿色**为可访问，**红色**为明确返回 404/410，**棕色**为浏览器无法判定
4. 红色链接可进入编辑模式删除或更新；棕色链接应先手动打开确认

> 提示：浏览器无法可靠区分 CORS、反爬、登录墙、网络策略与真实断网。因此这些情况会标为“无法判定”，而不会冒充确定的死链。

### 💾 功能 6：备份和恢复（换电脑必看）

**备份（导出）：**

1. 点左边栏的「**导出**」按钮
2. 在弹出的窗口里点「**下载文件**」
3. 得到一个 `.json` 文件（比如 `omnipage-2026-08-21.json`），**把它保存好**；可以放在导航文件旁，也可以存到网盘或 U 盘
4. 建议设置好网址后立即导出一次，避免清除浏览器数据时丢失编辑内容

**恢复（导入）：**

1. 在新电脑/新浏览器上打开 OmniPage
2. 点左边栏的「**导入**」按钮
3. 点「上传文件」，选择之前备份的那个 `.json` 文件
4. 页面会校验数据结构、数量、字段长度及 URL；确认预览数量后再完成导入

> ⚠️ 导入会覆盖当前数据。v1.0.1 会先在 localStorage 中保留上一份有效数据，但浏览器数据被整体清除时这份内部备份也会消失，因此不能替代导出的文件备份。

## 四、常见问题（FAQ）

**Q：我的数据存在哪里？安全吗？**
A：书签数据存在当前浏览器的 localStorage 中，OmniPage 本身没有接收数据的后端。搜索会打开所选搜索引擎，主动运行链接检查时浏览器会请求收藏的网址。共享电脑上的其他使用者、浏览器扩展或能访问同一浏览器配置的人仍可能接触本地数据，所以不要把密码、Token 等秘密写进书签字段。

**Q：清除浏览器缓存会丢数据吗？**
A：会！如果清理浏览器数据时勾选了「网站数据 / Cookie 和其他网站数据」，本页的数据会被清掉。所以**重要数据记得用「导出」功能备份**。

**Q：我把 index.html 挪了位置，书签打不开了怎么办？**
A：书签记录的是文件路径，文件移动后旧书签会失效。重新双击打开文件并收藏新路径即可。部分浏览器会按本地文件路径隔离 localStorage，因此移动前应先导出 JSON；若新路径看不到旧数据，再从 JSON 恢复。

**Q：可以把别人的备份文件导入吗？**
A：可以，但只应导入可信来源。v1.0.1 会拒绝畸形结构、超限内容及 `javascript:`、`data:` 等非 HTTP/HTTPS URL；导入前仍应查看分类和链接数量是否符合预期。

## 五、开发与验证

普通使用者无需安装任何依赖。参与开发时需要 Node.js 22.16 或更高的 LTS 版本：

```bash
npm ci
npx playwright install chromium
npm test
```

`npm test` 会运行 HTML 静态校验和真实 Chromium 回归测试。详细修复记录见 [CHANGELOG.md](CHANGELOG.md)。

## 六、许可证

MIT License — 任何人都可以自由使用、修改和分享这个项目。

如果觉得好用，可以给我一颗小星星！
